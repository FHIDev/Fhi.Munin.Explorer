category: Notes for hosts
- **No contract change: the test captures of the kilde list, a kilde with delkilder and a variable were re-taken from the live Explorer API.** This is a fixture-only change. `kilder.json`, `kilde-med-delkilder.json` and `variable.json` under `test/Fhi.Munin.Explorer.Tests/Testdata/` now carry `andelKodeverk`, `andelStatistikk`, `groupKey`, `groupSortOrder` and `sections` as the API sends them. Every one of those fields was already in `Fhi.Munin.Explorer.Contracts`, so nothing a host compiles against, configures or renders changes. `datasamling.json` was re-taken earlier, in Fhi.Metadata-c9x90. (Fhi.Metadata-quo1j)
