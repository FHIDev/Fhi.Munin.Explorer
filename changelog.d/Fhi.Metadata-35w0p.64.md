category: Changed
- **The result row's "Lagre i liste" button is quieter.** It wears
  `button-square--ghost-blue` rather than the filled `button-square--secondary`, the variant
  the saved-list view's own buttons already use. It is drawn once per result row, so a page of
  25 results drew 25 primary-weight controls competing with the variable names being scanned.
  No new class name and no new rule: both variants are Stiler's. (Fhi.Metadata-35w0p.64)
