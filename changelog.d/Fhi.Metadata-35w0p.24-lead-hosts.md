category: Notes for hosts
- **New class name `munin-explorer-lead` on the lead sentence under the Kodeverk heading, and
  Fhi.Helsedata.Stiler has no rule for it yet.** It is a `<p>` directly after the heading. Both sample
  stylesheets give it `margin: 0 0 8px`, the declaration Stiler already has for
  `munin-explorer-complete-record__lead`; without a rule it takes the host's default paragraph margins. The
  Stiler rule is Fhi.Metadata-3z2s3. (Fhi.Metadata-35w0p.24)
