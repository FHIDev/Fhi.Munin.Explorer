category: Changed
- **BREAKING: `VariableExplorer` is now the whole variabelutforsker — search, the reader's own
  variable lists behind Runa's two tabs, and the view in the address bar.** The component that used
  to carry that name is now `VariableSearch`, and `VariableExplorerWithUrlState` is gone: mount
  `VariableExplorer` instead, with the same parameters.
