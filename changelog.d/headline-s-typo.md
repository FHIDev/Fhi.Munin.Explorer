category: Fixed

- **Nine block headings now carry a class helsedata actually styles.** They wore `headline-sm`,
  which no stylesheet on helsedata.no defines, so they rendered at the browser's default size;
  the real name is `headline-s`. `KildeView` was corrected under Fhi.Metadata-e4bj2 and the rest
  were missed.
