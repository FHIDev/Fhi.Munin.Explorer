category: Notes for hosts

- The `munin-explorer-breadcrumb__clear` class name is gone: the control it dressed, the × that
  emptied the hierarchy, is no longer rendered. A host carrying a rule for it can drop it — nothing
  else changes, and `munin-explorer-breadcrumb` itself is unaffected apart from now carrying
  `role="navigation"` and the trail's name. (Fhi.Metadata-oj286)
